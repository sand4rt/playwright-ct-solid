{
  "name": "sand4rt",
  "version": "1.0.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sand4rt/playwright-ct-solid.git"
  },
  "bugs": {
    "url": "https://github.com/sand4rt/playwright-ct-solid/issues"
  },
  "homepage": "https://github.com/sand4rt/playwright-ct-solid#readme",
  "engines": {
    "node": ">=24.11.1",
    "pnpm": ">=10.22.0"
  },
  "scripts": {
    "test": "pnpm --stream -r test",
    "build": "pnpm --stream -r build",
    "release": "pnpm --stream -r release",
    "playwright:version": "node -e \"console.log(require('./playwright-ct-solid/package.json').dependencies['@playwright/experimental-ct-core'])\""
  }
}
